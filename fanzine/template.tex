%% PIXEL — Plantilla fanzine A5
%% Compilar con LuaLaTeX vía Pandoc

\documentclass[10pt,a5paper,twoside]{article}

%% ── Paquetes ──────────────────────────────────────────────────────────────
\usepackage{geometry}
\geometry{
  a5paper,
  top=18mm, bottom=20mm,
  inner=18mm, outer=14mm,
  headsep=8mm, footskip=10mm
}

\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{spanish}

%% Fuentes — Lora para cuerpo, JetBrains Mono para UI
%% Si no están, caemos a Latin Modern + Courier
\IfFontExistsTF{Lora}{
  \setmainfont{Lora}
}{
  \setmainfont{TeX Gyre Pagella}
}
\IfFontExistsTF{JetBrains Mono}{
  \setmonofont{JetBrains Mono}[Scale=0.82]
}{
  \IfFontExistsTF{Courier New}{
    \setmonofont{Courier New}[Scale=0.82]
  }{
    \setmonofont{TeX Gyre Cursor}[Scale=0.82]
  }
}

\usepackage{xcolor}
\definecolor{pixelred}{HTML}{CC0000}
\definecolor{muted}{HTML}{555555}
\definecolor{border}{HTML}{D0CCC4}

\usepackage{microtype}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{mdframed}
\usepackage{tocloft}

\hypersetup{
  colorlinks=true,
  linkcolor=pixelred,
  urlcolor=pixelred,
  pdftitle={PIXEL — Número $issue$},
}

%% ── Tipografía ────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\linespread{1.2}

%% Títulos de sección
\titleformat{\section}
  {\large\bfseries}
  {}
  {0pt}
  {}
  [\vspace{2pt}{\color{border}\hrule height 0.4pt}\vspace{4pt}]

\titleformat{\subsection}
  {\normalsize\bfseries\itshape}
  {}
  {0pt}
  {}

\titlespacing{\section}{0pt}{24pt}{8pt}
\titlespacing{\subsection}{0pt}{16pt}{4pt}

%% ── Cabecera y pie ────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}

\fancyhead[LE]{\small\texttt{\textcolor{muted}{PIXEL\textcolor{pixelred}{.} — Nº$issue$}}}
\fancyhead[RO]{\small\texttt{\textcolor{muted}{\leftmark}}}
\fancyfoot[C]{\small\texttt{\textcolor{muted}{\thepage}}}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\texttt{\textcolor{muted}{\thepage}}}
}

%% ── Blockquote ────────────────────────────────────────────────────────────
\renewenvironment{quote}{
  \begin{mdframed}[
    leftline=true, rightline=false, topline=false, bottomline=false,
    linewidth=2pt, linecolor=pixelred,
    innerleftmargin=10pt, innerrightmargin=0pt,
    innertopmargin=4pt, innerbottommargin=4pt,
    backgroundcolor=white
  ]
  \itshape\color{muted}
}{
  \end{mdframed}
}

%% ── Pandoc compatibility ──────────────────────────────────────────────────
\providecommand{\tightlist}{\setlength{\itemsep}{2pt}\setlength{\parskip}{2pt}}

%% ── Documento ─────────────────────────────────────────────────────────────
\begin{document}

%% ── Portada ───────────────────────────────────────────────────────────────
\thispagestyle{empty}
\vspace*{20mm}

{\fontsize{48}{52}\selectfont\textbf{PIXEL\textcolor{pixelred}{.}}}

\vspace{6mm}
{\large\texttt{\textcolor{muted}{Revista de videojuegos}}}

\vspace{16mm}
{\LARGE\textbf{Número $issue$}}

\vspace{4mm}
{\normalsize\texttt{\textcolor{muted}{$date$}}}

\vspace{20mm}
{\color{border}\hrule height 0.6pt}
\vspace{6mm}

{\small\textit{Escritura · Crítica · Cultura}}

\vfill

{\footnotesize\texttt{\textcolor{muted}{Fanzine imprimible en A4 doble cara · Doblar y grapar}}}

\newpage

%% ── Índice ────────────────────────────────────────────────────────────────
\thispagestyle{plain}

{\large\textbf{\texttt{ÍNDICE}}}
\vspace{4mm}
{\color{pixelred}\hrule height 1pt}
\vspace{6mm}

\renewcommand{\cftsecfont}{\normalsize}
\renewcommand{\cftsecpagefont}{\small\ttfamily\color{muted}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\setlength{\cftbeforesecskip}{4pt}

\tableofcontents

\newpage

%% ── Contenido ─────────────────────────────────────────────────────────────
$body$

%% ── Contraportada ─────────────────────────────────────────────────────────
\newpage
\thispagestyle{empty}
\vspace*{\fill}

\begin{center}
{\large\textbf{PIXEL\textcolor{pixelred}{.}}}\\[4mm]
{\small\texttt{\textcolor{muted}{Revista independiente de videojuegos}}}\\[2mm]
{\small\texttt{\textcolor{muted}{hola@pixel.blog}}}\\[6mm]
{\footnotesize\textcolor{muted}{Imprime en A4 doble cara · Dobla por la mitad · Grapa en el lomo}}
\end{center}

\vspace*{\fill}

\end{document}
