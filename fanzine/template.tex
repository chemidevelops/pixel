%% PIXEL — Plantilla fanzine A5
%% Compilar con LuaLaTeX vía Pandoc

\documentclass[10pt,a5paper,twoside]{article}

%% ── Paquetes ──────────────────────────────────────────────────────────────
\usepackage{geometry}
\geometry{
  a5paper,
  top=18mm, bottom=20mm,
  inner=18mm, outer=14mm,
  headsep=8mm, footskip=10mm
}

\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{spanish}

\IfFontExistsTF{Lora}{
  \setmainfont{Lora}
}{
  \setmainfont{TeX Gyre Pagella}
}
\IfFontExistsTF{JetBrains Mono}{
  \setmonofont{JetBrains Mono}[Scale=0.82]
}{
  \IfFontExistsTF{Courier New}{
    \setmonofont{Courier New}[Scale=0.82]
  }{
    \setmonofont{TeX Gyre Cursor}[Scale=0.82]
  }
}

\usepackage{xcolor}
\definecolor{pixelred}{HTML}{CC0000}
\definecolor{muted}{HTML}{555555}
\definecolor{border}{HTML}{D0CCC4}

\usepackage{microtype}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage[hidelinks, pdfpagelayout=SinglePage]{hyperref}
\usepackage{enumitem}
\usepackage{mdframed}
\usepackage{tocloft}

%% Solo secciones en el índice (sin subsecciones)
\setcounter{tocdepth}{1}

%% ── Tipografía ────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}
\linespread{1.2}

%% Títulos de artículo más prominentes
\titleformat{\section}
  {\fontsize{15}{18}\selectfont\bfseries}
  {}
  {0pt}
  {}
  [\vspace{3pt}{\color{border}\hrule height 0.4pt}\vspace{6pt}]

\titleformat{\subsection}
  {\normalsize\bfseries\itshape}
  {}
  {0pt}
  {}

\titlespacing{\section}{0pt}{28pt}{10pt}
\titlespacing{\subsection}{0pt}{16pt}{4pt}

%% ── Cabecera y pie ────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}

\fancyhead[LE]{\small\texttt{\textcolor{muted}{PIXEL — Nº$issue$}}}
\fancyhead[RO]{\small\texttt{\textcolor{muted}{\leftmark}}}
\fancyfoot[C]{\small\texttt{\textcolor{muted}{\thepage}}}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[C]{\small\texttt{\textcolor{muted}{\thepage}}}
}

%% ── Blockquote ────────────────────────────────────────────────────────────
\renewenvironment{quote}{
  \begin{mdframed}[
    leftline=true, rightline=false, topline=false, bottomline=false,
    linewidth=2pt, linecolor=black,
    innerleftmargin=10pt, innerrightmargin=0pt,
    innertopmargin=4pt, innerbottommargin=4pt,
    backgroundcolor=white
  ]
  \itshape\color{muted}
}{
  \end{mdframed}
}

%% ── Pandoc compatibility ──────────────────────────────────────────────────
\providecommand{\tightlist}{\setlength{\itemsep}{2pt}\setlength{\parskip}{2pt}}

%% ── Documento ─────────────────────────────────────────────────────────────
\begin{document}

%% ── Portada delantera (mitad derecha de la imagen horizontal) ─────────────
\thispagestyle{empty}
\newgeometry{margin=0pt}
\noindent\includegraphics[width=\paperwidth,height=\paperheight]{$cover_front$}
\restoregeometry
\newpage

%% ── Tras la portada: todo en B/N ──────────────────────────────────────────
\definecolor{pixelred}{HTML}{000000}
\definecolor{muted}{HTML}{555555}
\definecolor{border}{HTML}{AAAAAA}
\hypersetup{linkcolor=black, urlcolor=black}

%% ── Índice ────────────────────────────────────────────────────────────────
\thispagestyle{plain}

{\large\textbf{\texttt{ÍNDICE}}}
\vspace{4mm}
{\color{black}\hrule height 1pt}
\vspace{6mm}

\renewcommand{\cftsecfont}{\normalsize}
\renewcommand{\cftsecpagefont}{\small\ttfamily\color{muted}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\setlength{\cftbeforesecskip}{4pt}

\renewcommand{\contentsname}{}
\tableofcontents

\newpage

%% ── Contenido ─────────────────────────────────────────────────────────────
$body$

%% ── Contraportada (mitad izquierda de la imagen horizontal) ──────────────
\cleardoublepage
\thispagestyle{empty}
\newgeometry{margin=0pt}
\noindent\includegraphics[width=\paperwidth,height=\paperheight]{$cover_back$}
\restoregeometry

\end{document}
