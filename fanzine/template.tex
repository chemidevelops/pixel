%% PIXEL — Plantilla fanzine A5 — Estilo brutalist/independiente
%% Compilar con LuaLaTeX vía Pandoc

\documentclass[10pt,a5paper,twoside]{article}

\usepackage{geometry}
\geometry{
  a5paper,
  top=16mm, bottom=18mm,
  inner=16mm, outer=12mm,
  headsep=6mm, footskip=10mm
}

\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{spanish}

\IfFontExistsTF{Lora}{
  \setmainfont{Lora}
}{
  \setmainfont{TeX Gyre Pagella}
}
\IfFontExistsTF{JetBrains Mono}{
  \setmonofont{JetBrains Mono}[Scale=0.82]
}{
  \IfFontExistsTF{Courier New}{
    \setmonofont{Courier New}[Scale=0.82]
  }{
    \setmonofont{TeX Gyre Cursor}[Scale=0.82]
  }
}

\usepackage{xcolor}
\definecolor{pixelred}{HTML}{CC0000}
\definecolor{muted}{HTML}{666666}
\definecolor{border}{HTML}{BBBBBB}

\usepackage{microtype}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{needspace}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage[hidelinks, pdfpagelayout=SinglePage]{hyperref}
\usepackage{enumitem}
\usepackage{mdframed}
\usepackage{tocloft}
\usepackage{lettrine}

%% Solo secciones en el índice
\setcounter{tocdepth}{1}

%% ── Tipografía ────────────────────────────────────────────────────────────
\setlength{\parindent}{0pt}
\setlength{\parskip}{7pt}
\linespread{1.22}

%% Títulos de artículo — estilo brutalist
\titleformat{\section}[block]
  {}
  {}
  {0pt}
  {%
    \needspace{6\baselineskip}%
    \fontsize{8}{10}\selectfont\texttt{\textbf{— \thesection\ —}}\\[3pt]%
    \fontsize{20}{22}\selectfont\bfseries%
  }
  [\vspace{5pt}{\hrule height 1.5pt}\vspace{10pt}]

\titleformat{\subsection}[block]
  {}
  {}
  {0pt}
  {%
    \needspace{4\baselineskip}%
    \normalsize\bfseries\itshape%
  }

\titlespacing{\section}{0pt}{28pt}{0pt}
\titlespacing{\subsection}{0pt}{18pt}{4pt}

%% Drop cap
\setlength{\DefaultFindent}{3pt}
\setlength{\DefaultNindent}{0pt}
\renewcommand{\LettrineFontHook}{\bfseries}

%% ── Imágenes ──────────────────────────────────────────────────────────────
\setkeys{Gin}{width=\linewidth, keepaspectratio}
\setlength{\intextsep}{10pt}

%% ── Cabecera y pie ────────────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}

\fancyhead[LE]{\fontsize{7}{9}\selectfont\texttt{PIXEL — Nº$issue$}}
\fancyhead[RO]{\fontsize{7}{9}\selectfont\texttt{\leftmark}}
\fancyfoot[LE,RO]{\fontsize{7}{9}\selectfont\texttt{\thepage}}

\fancypagestyle{plain}{
  \fancyhf{}
  \fancyfoot[LE,RO]{\fontsize{7}{9}\selectfont\texttt{\thepage}}
}

%% ── Blockquote / pull quote ───────────────────────────────────────────────
\renewenvironment{quote}{
  \vspace{6pt}
  \begin{mdframed}[
    leftline=true, rightline=false, topline=false, bottomline=false,
    linewidth=3pt, linecolor=black,
    innerleftmargin=10pt, innerrightmargin=0pt,
    innertopmargin=2pt, innerbottommargin=2pt,
    backgroundcolor=white
  ]
  \fontsize{12}{15}\selectfont\bfseries\itshape
}{
  \end{mdframed}
  \vspace{6pt}
}

%% ── Pandoc compatibility ──────────────────────────────────────────────────
\providecommand{\tightlist}{\setlength{\itemsep}{2pt}\setlength{\parskip}{2pt}}
\providecommand{\pandocbounded}[1]{#1}

%% ── Documento ─────────────────────────────────────────────────────────────
\begin{document}

%% ── Portada delantera ─────────────────────────────────────────────────────
\thispagestyle{empty}
\newgeometry{margin=0pt}
\noindent\includegraphics[width=\paperwidth,height=\paperheight,keepaspectratio=false]{$cover_front$}
\restoregeometry
\newpage

%% ── Tras la portada: todo en B/N ──────────────────────────────────────────
\definecolor{pixelred}{HTML}{000000}
\definecolor{muted}{HTML}{666666}
\definecolor{border}{HTML}{AAAAAA}
\hypersetup{linkcolor=black, urlcolor=black}

%% ── Índice + Editorial ────────────────────────────────────────────────────
\thispagestyle{plain}

{\fontsize{9}{11}\selectfont\texttt{\textbf{ÍNDICE}}}
\vspace{3mm}
{\hrule height 1.5pt}
\vspace{5mm}

\renewcommand{\cftsecfont}{\normalsize}
\renewcommand{\cftsecpagefont}{\small\ttfamily\color{muted}}
\renewcommand{\cftsecleader}{\cftdotfill{\cftdotsep}}
\setlength{\cftbeforesecskip}{5pt}
\renewcommand{\contentsname}{}
\tableofcontents

\vfill

{\hrule height 0.4pt}
\vspace{4mm}
{\fontsize{8}{11}\selectfont\textit{Este es el primer número de PIXEL, una revista independiente sobre videojuegos. Aquí no hay anunciantes, algoritmos ni listas de los más vendidos. Solo escritura sobre juegos que merecen ser pensados en voz alta. Bienvenido.}}

\newpage

%% ── Contenido ─────────────────────────────────────────────────────────────
$body$

%% ── Contraportada ─────────────────────────────────────────────────────────
\newpage
\thispagestyle{empty}
\newgeometry{margin=0pt}
\noindent\includegraphics[width=\paperwidth,height=\paperheight,keepaspectratio=false]{$cover_back$}
\restoregeometry

\end{document}
